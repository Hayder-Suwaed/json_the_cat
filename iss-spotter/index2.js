const { nextISSTimesForMyLocation } = require('./iss_promised');
nextISSTimesForMyLocation()
    .then((passTimes) => {
        for (let timeof ),
      console.log(PassTimes(passTimes));
    })nextISSTimesForMyLocationc
  